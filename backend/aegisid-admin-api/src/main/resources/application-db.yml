spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aegisid
    username: aegisid
    password: aegisid
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

