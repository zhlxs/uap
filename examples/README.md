# Integration Examples

This directory will hold runnable examples for business-system integration:

- `spring-boot-oidc-client`: standard OIDC client integration.
- `gateway-header-client`: legacy application integration through a trusted gateway.

